{application, poolboy, [
    {description, "A hunky Erlang worker pool factory"},
    {vsn, {cmd, "echo `cat VERSION`"}},
    {modules, []},
    {applications, [kernel, stdlib]},
    {registered, [poolboy]}
]}.
