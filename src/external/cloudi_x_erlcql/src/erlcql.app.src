%% -*- mode: erlang -*-
%% vi: set ft=erlang :

{application, erlcql,
 [{description, "Cassandra native protocol CQL client"},
  {vsn, "0.1.6"},
  {applications,
   [kernel,
    stdlib,
    snappy,
    lz4,
    backoff]}]}.
